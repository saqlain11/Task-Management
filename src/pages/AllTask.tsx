import { Layout, TaskTable } from "components";
import React from "react";

const AllTask: React.FC = () => {
  return (
    <Layout>
      <TaskTable />
    </Layout>
  );
};

export default AllTask;
